// next.config.ts
import { NextConfig } from "next";

const config: NextConfig = {
  experimental: {
    // turbopack: false,  // Remove this line if you don't need Turbopack
  },
};

export default config;
